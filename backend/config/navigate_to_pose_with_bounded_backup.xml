<!--
  SPDX-License-Identifier: Apache-2.0
  Adapted from Navigation2 1.3.12:
  nav2_bt_navigator/behavior_trees/navigate_to_pose_w_replanning_and_recovery.xml
  Upstream commit: 6be3614013ec586051b86c97b919b293281490fe
  Changes: removed the broad recovery round-robin and replaced it with one
  bounded BackUp plus local/global costmap clearing; Spin and Wait are absent.
  See docs/DEPENDENCY_LICENSE_AUDIT.md, LICENSES/Apache-2.0.txt, and
  LICENSES/Navigation2-LICENSE.txt.

  Replan at 1 Hz. If FollowPath cannot make 0.08 m progress for 5 seconds,
  back straight along the approach heading once. BackUp performs local-costmap
  collision prediction and publishes through /cmd_vel_nav -> Command Gate ->
  Safety. After the retreat, clear both costmaps and retry the current path once.
  A second controller failure aborts the Action so exploration can blacklist
  this frontier and select another candidate. Spin is intentionally absent.
-->
<root BTCPP_format="4" main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <PipelineSequence name="NavigateWithReplanning">
      <ControllerSelector selected_controller="{selected_controller}" default_controller="FollowPath" topic_name="controller_selector"/>
      <PlannerSelector selected_planner="{selected_planner}" default_planner="GridBased" topic_name="planner_selector"/>
      <RateController hz="1.0">
        <RecoveryNode number_of_retries="1" name="ComputePathToPose">
          <ComputePathToPose goal="{goal}" path="{path}" planner_id="{selected_planner}" error_code_id="{compute_path_error_code}"/>
          <Sequence>
            <WouldAPlannerRecoveryHelp error_code="{compute_path_error_code}"/>
            <ClearEntireCostmap name="ClearGlobalCostmap-Context" service_name="global_costmap/clear_entirely_global_costmap"/>
          </Sequence>
        </RecoveryNode>
      </RateController>
      <RecoveryNode number_of_retries="1" name="FollowPath">
        <FollowPath path="{path}" controller_id="{selected_controller}" error_code_id="{follow_path_error_code}"/>
        <Sequence name="BackOutBeforeReplanning">
          <WouldAControllerRecoveryHelp error_code="{follow_path_error_code}"/>
          <BackUp backup_dist="0.40" backup_speed="0.12" time_allowance="4.5" error_code_id="{backup_error_code}"/>
          <ClearEntireCostmap name="ClearLocalCostmap-AfterBackUp" service_name="local_costmap/clear_entirely_local_costmap"/>
          <ClearEntireCostmap name="ClearGlobalCostmap-AfterBackUp" service_name="global_costmap/clear_entirely_global_costmap"/>
        </Sequence>
      </RecoveryNode>
    </PipelineSequence>
  </BehaviorTree>
</root>
